import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.alfredt.toughtimer',
  appName: 'Tough Timer',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
